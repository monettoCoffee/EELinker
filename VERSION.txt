EELinkder Version Document

更新内容文档
0.1.0: 20190419
    Model:
        Router, ORM, Container

0.1.1: 20190520
    ORM:
        添加了示例：
            Select/Insert/Delete/Update
        修复相同的SQL参数只能使用一次的问题。
        修复SQL语句本身具有转义符号而引起的异常问题。
        Mapper标签忽略大小写。
        Mapper支持自定义标签。
        
        Add sameple:
            Select/Insert/Delete/Update
        Fix bug: the same sql parameter can only use once.
        Fix bug: special orm character cause exception.
        Mapper label ignore case.
        Mapper supports custom label.
        

0.1.2: Waitting...
    Plan:
        支持通过注解添加SQL Mapper。
        支持Router参数默认值，参数默认值本身为等号的逻辑暂未处理。
        Dispatcher不要用SimpleName。
        ORM在进行连接时密码不对时的提醒。